import { defineMessages } from 'react-intl';

const messages = defineMessages({
  settings_DialogHeader_title: {
    id: 'app.DialogHeader.title',
    defaultMessage: 'Settings',
    description: 'Application settings'
  }
});

export default messages;
